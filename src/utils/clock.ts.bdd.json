{
  "$schema": "https://coderrob.com/schemas/bdd-spec.schema.json",
  "schemaVersion": "1.0.0",
  "sourceFile": "src//utils//clock.ts",
  "module": {
    "name": "clock",
    "description": "Behavioral contract for src//utils//clock.ts",
    "exports": ["Clock", "IClock"]
  },
  "specifications": [
    {
      "feature": "Clock helpers",
      "scenarios": [
        {
          "name": "should expose monotonic time helpers",
          "given": "the utility module is imported",
          "when": "callers request monotonic timestamps or collision-resistant monotonic tokens",
          "then": "the module should return values derived from monotonic clocks without relying on Date.now"
        }
      ]
    }
  ]
}
