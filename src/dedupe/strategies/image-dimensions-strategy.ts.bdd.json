{
  "$schema": "https:/coderrob.com/schemas/bdd-spec.schema.json",
  "schemaVersion": "1.0.0",
  "sourceFile": "src/dedupe/strategies/image-dimensions-strategy.ts",
  "module": {
    "name": "image-dimensions-strategy",
    "description": "Behavioral contract for src/dedupe/strategies/image-dimensions-strategy.ts",
    "exports": ["ImageDimensionsStrategy"]
  },
  "specifications": [
    {
      "feature": "Source contract for image-dimensions-strategy",
      "scenarios": [
        {
          "name": "should satisfy the documented module contract",
          "given": "the module is imported by consumers",
          "when": "its public API is used according to the source definition",
          "then": "the documented behavior should remain consistent"
        }
      ]
    }
  ]
}
